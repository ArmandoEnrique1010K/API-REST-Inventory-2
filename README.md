# API-REST-Inventory-2

El backend está diseñado como un monolito modular. Cada módulo encapsula su dominio y mantiene una arquitectura en capas. Los servicios compartidos se ubican en un módulo common reducido, evitando acoplamiento innecesario.
